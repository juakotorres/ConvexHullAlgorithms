Como ejecutar el proyecto:
En caso de tener CLion abrir proyecto y ejecutar normalmente con el IDE.

En caso de querer ejecutar en linea de comando, ir a la carpeta del proyecto y ejecutar lo siguiente
$cmake ./
$make
$./Tarea1

Esto ejecutará lo siguiente:

Crea una nube de puntos.
Con el primer algoritmo calcula la cerradura convexa y calcula el tiempo tomado (GiftWrapping).
Lo mismo con el segundo algoritmo (QuickHull).
Verifica si las cerraduras son las mismas.
Entrega los resultados finalmente en pantalla.